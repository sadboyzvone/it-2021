<?php

/**
 * Resolves the title based on the URI.
 */
class TitleService {

    /**
     * Resolves the title based on the URI.
     *
     * @return string
     *   Title.
     */
    public static function resolve(): string {
        return 'Homepage';
    }

}